<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <% if @group.errors.any? %>

  <div id="error_explanation">
    <h3 class="text-danger"><%= pluralize(@group.errors.count, "error") %> prohibited this obj from being saved:</h3>
    <ul class="text-danger">
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  		<h1>Create Group</h1>
  		  <%= render 'group_form', group: @group %>


  </div>
</div>